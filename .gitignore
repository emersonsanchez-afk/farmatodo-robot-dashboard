# Credenciales y configuración local (usa config.example.yaml como plantilla)
config.yaml
service_account.json

# robot_history.json e index.html SÍ se versionan: el workflow diario los
# commitea para persistir el historial entre corridas y para que Netlify/
# GitHub Pages sirvan el dashboard actualizado directamente desde el repo.
_tmp_tasks_*.xlsx
robot_dashboard.log

# Python
__pycache__/
*.pyc
.venv/
venv/
